# IMPORTANT: Do NOT use quotes in .env files for Docker!
# Docker doesn't strip quotes - they become part of the value

# Anthropic API Key (Required - choose ONE of the options below)
# Option 1 (Recommended): Standard API Key from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-YOUR-API-KEY-HERE

# Option 2 (Requires Anthropic Permission): Claude Code OAuth Token
# Only use this if you have prior approval from Anthropic for OAuth usage
# Get this from: claude setup-token (requires @anthropic-ai/claude-code installed)
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-YOUR-OAUTH-TOKEN-HERE

# API Key for authentication (optional but recommended for REST API)
# Generate with: openssl rand -hex 32
CLAUDE_AGENT_SDK_CONTAINER_API_KEY=your-api-key-here-no-quotes

# Clerk Authentication (REQUIRED)
# Get both keys at: https://dashboard.clerk.com/last-active?path=api-keys
CLERK_SECRET_KEY=sk_test_YOUR-CLERK-SECRET-KEY-HERE
CLERK_PUBLISHABLE_KEY=pk_test_YOUR-CLERK-PUBLISHABLE-KEY-HERE

# Port (optional, defaults to 8080)
# PORT=8080
